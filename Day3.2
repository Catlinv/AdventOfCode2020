def solve():
    rez = 1
    r = [1, 3, 5, 7, 1]
    d = [1, 1, 1, 1, 2]
    file1 = open('in.txt', 'r')
    file2 = open('out.txt', 'w')
    A = file1.readlines()
    for i in range(0, 5):
        nr = 0
        pos_x = 0
        pos_y = 0
        while pos_y + d[i] < len(A):
            pos_x = (pos_x + r[i]) % (len(A[0]) - 1)
            pos_y = pos_y + d[i]
            nr += A[pos_y][pos_x] == "#"
        print(nr)
        rez *= nr
    print(rez)


if __name__ == '__main__':
    solve()
